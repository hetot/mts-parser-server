package com.example.mtstestproject.repository;

import com.example.mtstestproject.model.TariffBenefitEntity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TariffBenefitRepository extends JpaRepository<TariffBenefitEntity, Long> {
}
